'use strict'; const STORAGE_KEY = 'enkel-todo-github-v1'; const PROFILE_KEY = 'enkel-todo-profile'; const POLL_INTERVAL = 20000; const GITHUB = { owner: 'enkeldesign', repo: 'webb', path: 'todo/data.json', branch: 'main' }; const state = { actor: '', tasks: [], sortKey: 'priority', sortDirection: 'asc', draggedId: null, loading: false, saving: false, pollTimer: null }; const elements = {}; window.addEventListener('DOMContentLoaded', () => { cacheElements(); bindEvents(); updateDirectionButton(); elements.settingsButton.textContent = 'Redigeringsåtkomst'; openProfileDialog(); }); function cacheElements() { const ids = [ 'profileButton', 'addButton', 'sortSelect', 'directionButton', 'refreshButton', 'settingsButton', 'syncStatus', 'taskBody', 'emptyState', 'profileDialog', 'taskDialog', 'taskForm', 'taskDialogHeading', 'taskId', 'titleInput', 'nextStepInput', 'urgencyInput', 'valueInput', 'calmInput', 'energyInput', 'tagsInput', 'statusInput', 'settingsDialog', 'settingsForm', 'tokenInput', 'forgetSettingsButton', 'taskRowTemplate' ]; for (const id of ids) elements[id] = document.getElementById(id); } function bindEvents() { document.querySelectorAll('[data-profile]').forEach(button => { button.addEventListener('click', () => chooseProfile(button.dataset.profile)); }); document.querySelectorAll('[data-close-dialog]').forEach(button => { button.addEventListener('click', () => document.getElementById(button.dataset.closeDialog).close()); }); elements.profileDialog.addEventListener('cancel', event => event.preventDefault()); elements.profileButton.addEventListener('click', openProfileDialog); elements.addButton.addEventListener('click', () => beginEdit()); elements.settingsButton.addEventListener('click', openSettingsDialog); elements.refreshButton.addEventListener('click', () => loadTasks({ announce: true })); elements.sortSelect.addEventListener('change', handleSortChange); elements.directionButton.addEventListener('click', toggleSortDirection); elements.taskForm.addEventListener('submit', saveTask); elements.settingsForm.addEventListener('submit', saveSettings); elements.forgetSettingsButton.addEventListener('click', forgetSettings); document.addEventListener('visibilitychange', () => { if (!document.hidden && state.actor) loadTasks({ announce: false }); }); } function openProfileDialog() { if (!elements.profileDialog.open) elements.profileDialog.showModal(); } async function chooseProfile(profile) { state.actor = profile; sessionStorage.setItem(PROFILE_KEY, profile); elements.profileButton.textContent = profile; elements.profileDialog.close(); await loadTasks({ announce: false }); startPolling(); } function getToken() { try { return JSON.parse(localStorage.getItem(STORAGE_KEY))?.token?.trim() || ''; } catch { return ''; } } function openSettingsDialog() { elements.tokenInput.value = getToken(); if (!elements.settingsDialog.open) elements.settingsDialog.showModal(); } async function saveSettings(event) { event.preventDefault(); const token = elements.tokenInput.value.trim(); localStorage.setItem(STORAGE_KEY, JSON.stringify({ token })); elements.settingsDialog.close(); setSyncStatus('Redigeringsåtkomst sparad på den här enheten.'); renderTasks(); } function forgetSettings() { if (!confirm('Glömma GitHub-token på den här enheten?')) return; localStorage.removeItem(STORAGE_KEY); elements.settingsDialog.close(); setSyncStatus('Token borttagen. Listan går fortfarande att läsa.'); renderTasks(); } function beginEdit(task = null) { if (!getToken()) { setSyncStatus('Lägg in en GitHub-token för att redigera listan.'); openSettingsDialog(); return; } openTaskDialog(task); } function publicDataUrl() { return `./data.json?cache=${Date.now()}`; } function githubUrl() { const encodedPath = GITHUB.path.split('/').map(encodeURIComponent).join('/'); return `https://api.github.com/repos/${GITHUB.owner}/${GITHUB.repo}/contents/${encodedPath}`; } function githubHeaders() { const token = getToken(); if (!token) throw new Error('MISSING_TOKEN'); return { Accept: 'application/vnd.github+json', Authorization: `Bearer ${token}`, 'X-GitHub-Api-Version': '2022-11-28' }; } async function requestGitHub(url, options = {}) { const response = await fetch(url, { ...options, headers: { ...githubHeaders(), ...(options.headers ?? {}) } }); if (!response.ok) { let detail = ''; try { const error = await response.json(); detail = error.message ?? ''; } catch { detail = await response.text(); } const requestError = new Error(detail || `GitHub-fel ${response.status}`); requestError.status = response.status; throw requestError; } if (response.status === 204) return null; return response.json(); } async function fetchPublicData() { const response = await fetch(publicDataUrl(), { cache: 'no-store' }); if (!response.ok) throw new Error(`Kunde inte läsa listan (${response.status}).`); const data = await response.json(); if (!Array.isArray(data.tasks)) throw new Error('Datafilen saknar en uppgiftslista.'); return data; } async function fetchWritableData() { const result = await requestGitHub(`${githubUrl()}?ref=${encodeURIComponent(GITHUB.branch)}`); const data = JSON.parse(decodeBase64(result.content)); if (!Array.isArray(data.tasks)) throw new Error('Datafilen saknar en uppgiftslista.'); return { data, sha: result.sha }; } async function loadTasks({ announce = false } = {}) { if (state.loading || state.saving) return; state.loading = true; setBusy(true); try { const data = await fetchPublicData(); state.tasks = normalizeTasks(data.tasks); renderTasks(); setSyncStatus(`Synkad ${formatTime(new Date())}.`); if (announce) announceStatus('Listan är uppdaterad.'); } catch (error) { setSyncStatus(`Kunde inte synka: ${friendlyError(error)}`); } finally { state.loading = false; setBusy(false); } } async function mutateRemote(mutator, commitMessage) { if (!getToken()) { openSettingsDialog(); throw new Error('MISSING_TOKEN'); } if (state.saving) throw new Error('En annan ändring sparas fortfarande.'); state.saving = true; setBusy(true); setSyncStatus('Sparar till GitHub…'); try { for (let attempt = 0; attempt < 2; attempt += 1) { const remote = await fetchWritableData(); const draft = JSON.parse(JSON.stringify(remote.data)); draft.tasks = normalizeTasks(draft.tasks); mutator(draft.tasks); draft.tasks = normalizeTasks(draft.tasks); draft.tasks.forEach((task, index) => { task.priority = index + 1; }); draft.version = 1; draft.updated_at = new Date().toISOString(); draft.updated_by = state.actor; try { await requestGitHub(githubUrl(), { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ message: `${commitMessage} (${state.actor})`, content: encodeBase64(`${JSON.stringify(draft, null, 2)}\n`), sha: remote.sha, branch: GITHUB.branch }) }); state.tasks = draft.tasks; renderTasks(); setSyncStatus(`Sparad ${formatTime(new Date())}.`); return; } catch (error) { if (error.status === 409 && attempt === 0) continue; throw error; } } } finally { state.saving = false; setBusy(false); } } function normalizeTasks(tasks) { return [...tasks] .map(task => ({ ...task, id: String(task.id), priority: Number(task.priority) || 9999, urgency: clampRating(task.urgency), value_creation: clampRating(task.value_creation), calm: clampRating(task.calm), energy: clampRating(task.energy), tags: Array.isArray(task.tags) ? task.tags : [], status: ['todo', 'doing', 'done'].includes(task.status) ? task.status : 'todo' })) .sort((a, b) => a.priority - b.priority); } function clampRating(value) { return Math.min(5, Math.max(1, Number(value) || 3)); } function handleSortChange() { state.sortKey = elements.sortSelect.value; state.sortDirection = state.sortKey === 'priority' || state.sortKey === 'title' ? 'asc' : 'desc'; updateDirectionButton(); renderTasks(); } function toggleSortDirection() { if (state.sortKey === 'priority') return; state.sortDirection = state.sortDirection === 'asc' ? 'desc' : 'asc'; updateDirectionButton(); renderTasks(); } function updateDirectionButton() { const isPriority = state.sortKey === 'priority'; elements.directionButton.disabled = isPriority; elements.directionButton.textContent = state.sortDirection === 'asc' ? 'Stigande' : 'Fallande'; elements.directionButton.setAttribute('aria-pressed', String(state.sortDirection === 'desc')); } function calculateScore(task) { return task.urgency * 5 + task.calm * 3 + task.value_creation * 2 - task.energy * 2; } function sortedTasks() { const tasks = [...state.tasks]; const direction = state.sortDirection === 'asc' ? 1 : -1; return tasks.sort((a, b) => { let aValue; let bValue; switch (state.sortKey) { case 'score': aValue = calculateScore(a); bValue = calculateScore(b); break; case 'value': aValue = a.value_creation; bValue = b.value_creation; break; case 'title': return a.title.localeCompare(b.title, 'sv') * direction; case 'status': aValue = statusOrder(a.status); bValue = statusOrder(b.status); break; case 'updated_at': aValue = new Date(a.updated_at).getTime(); bValue = new Date(b.updated_at).getTime(); break; default: aValue = a[state.sortKey]; bValue = b[state.sortKey]; } if (aValue === bValue) return a.priority - b.priority; return (aValue - bValue) * direction; }); } function renderTasks() { const tasks = sortedTasks(); elements.taskBody.replaceChildren(); elements.emptyState.hidden = tasks.length > 0; const canReorder = state.sortKey === 'priority' && Boolean(getToken()); tasks.forEach((task, visibleIndex) => { const row = elements.taskRowTemplate.content.firstElementChild.cloneNode(true); row.dataset.id = task.id; row.draggable = canReorder; row.querySelector('.priority-number').textContent = task.priority; row.querySelector('.task-title').textContent = task.title; row.querySelector('.next-step').textContent = task.next_step || 'Inte angivet'; setRating(row.querySelector('.urgency'), task.urgency, 'Brådska'); setRating(row.querySelector('.value'), task.value_creation, 'Värdeskapande'); setRating(row.querySelector('.calm'), task.calm, 'Lugnvärde'); setRating(row.querySelector('.energy'), task.energy, 'Energikostnad'); row.querySelector('.score').textContent = calculateScore(task); renderTags(row.querySelector('.tags'), task.tags); renderStatus(row.querySelector('.status'), task); const upButton = row.querySelector('.move-up'); const downButton = row.querySelector('.move-down'); upButton.disabled = !canReorder || visibleIndex === 0; downButton.disabled = !canReorder || visibleIndex === tasks.length - 1; upButton.addEventListener('click', () => moveTask(task.id, -1)); downButton.addEventListener('click', () => moveTask(task.id, 1)); row.querySelector('.edit-button').addEventListener('click', () => beginEdit(task)); row.querySelector('.delete-button').addEventListener('click', () => deleteTask(task)); row.addEventListener('dragstart', handleDragStart); row.addEventListener('dragover', handleDragOver); row.addEventListener('dragleave', handleDragLeave); row.addEventListener('drop', handleDrop); row.addEventListener('dragend', handleDragEnd); elements.taskBody.append(row); }); } function setRating(cell, value, label) { cell.textContent = `${value}/5`; cell.setAttribute('aria-label', `${label}: ${value} av 5`); } function renderTags(cell, tags = []) { cell.replaceChildren(); if (!tags.length) { cell.textContent = 'Inga'; return; } for (const tag of tags) { const chip = document.createElement('span'); chip.className = 'tag'; chip.textContent = tag; cell.append(chip); } } function renderStatus(cell, task) { const badge = document.createElement('span'); badge.className = 'status-badge'; badge.textContent = statusLabel(task.status); badge.title = `Senast ändrad av ${task.updated_by ?? 'okänd'} ${formatDateTime(task.updated_at)}`; cell.replaceChildren(badge); } function openTaskDialog(task = null) { elements.taskForm.reset(); elements.taskId.value = task?.id ?? ''; elements.taskDialogHeading.textContent = task ? 'Redigera uppgift' : 'Lägg till uppgift'; elements.titleInput.value = task?.title ?? ''; elements.nextStepInput.value = task?.next_step ?? ''; elements.urgencyInput.value = task?.urgency ?? 3; elements.valueInput.value = task?.value_creation ?? 3; elements.calmInput.value = task?.calm ?? 3; elements.energyInput.value = task?.energy ?? 3; elements.tagsInput.value = task?.tags?.join(', ') ?? ''; elements.statusInput.value = task?.status ?? 'todo'; elements.taskDialog.showModal(); elements.titleInput.focus(); } async function saveTask(event) { event.preventDefault(); const submitButton = elements.taskForm.querySelector('[type="submit"]'); submitButton.disabled = true; const id = elements.taskId.value; const now = new Date().toISOString(); const values = { title: elements.titleInput.value.trim(), next_step: elements.nextStepInput.value.trim(), urgency: Number(elements.urgencyInput.value), value_creation: Number(elements.valueInput.value), calm: Number(elements.calmInput.value), energy: Number(elements.energyInput.value), tags: parseTags(elements.tagsInput.value), status: elements.statusInput.value, updated_at: now, updated_by: state.actor }; try { await mutateRemote(tasks => { if (id) { const index = tasks.findIndex(task => task.id === id); if (index < 0) throw new Error('Uppgiften finns inte längre. Uppdatera listan och försök igen.'); tasks[index] = { ...tasks[index], ...values }; } else { tasks.push({ id: crypto.randomUUID(), priority: tasks.length + 1, created_at: now, created_by: state.actor, ...values }); } }, id ? 'Redigera uppgift' : 'Lägg till uppgift'); elements.taskDialog.close(); } catch (error) { setSyncStatus(`Kunde inte spara: ${friendlyError(error)}`); } finally { submitButton.disabled = false; } } async function deleteTask(task) { if (!getToken()) { setSyncStatus('Lägg in en GitHub-token för att ändra listan.'); openSettingsDialog(); return; } if (!confirm(`Ta bort ”${task.title}”?`)) return; try { await mutateRemote(tasks => { const index = tasks.findIndex(candidate => candidate.id === task.id); if (index < 0) throw new Error('Uppgiften finns inte längre.'); tasks.splice(index, 1); }, `Ta bort uppgift: ${task.title}`); } catch (error) { setSyncStatus(`Kunde inte ta bort: ${friendlyError(error)}`); } } async function moveTask(id, offset) { if (state.sortKey !== 'priority' || !getToken()) { if (!getToken()) openSettingsDialog(); return; } const ordered = [...state.tasks].sort((a, b) => a.priority - b.priority); const index = ordered.findIndex(task => task.id === id); const targetIndex = index + offset; if (index < 0 || targetIndex < 0 || targetIndex >= ordered.length) return; [ordered[index], ordered[targetIndex]] = [ordered[targetIndex], ordered[index]]; await persistOrder(ordered); } function handleDragStart(event) { if (state.sortKey !== 'priority' || !getToken()) { event.preventDefault(); return; } state.draggedId = event.currentTarget.dataset.id; event.currentTarget.classList.add('dragging'); event.dataTransfer.effectAllowed = 'move'; } function handleDragOver(event) { if (!state.draggedId || state.sortKey !== 'priority') return; event.preventDefault(); event.currentTarget.classList.add('drag-target'); event.dataTransfer.dropEffect = 'move'; } function handleDragLeave(event) { event.currentTarget.classList.remove('drag-target'); } async function handleDrop(event) { event.preventDefault(); const targetId = event.currentTarget.dataset.id; event.currentTarget.classList.remove('drag-target'); if (!state.draggedId || state.draggedId === targetId) return; const ordered = [...state.tasks].sort((a, b) => a.priority - b.priority); const fromIndex = ordered.findIndex(task => task.id === state.draggedId); const toIndex = ordered.findIndex(task => task.id === targetId); if (fromIndex < 0 || toIndex < 0) return; const [moved] = ordered.splice(fromIndex, 1); ordered.splice(toIndex, 0, moved); await persistOrder(ordered); } function handleDragEnd(event) { event.currentTarget.classList.remove('dragging'); document.querySelectorAll('.drag-target').forEach(row => row.classList.remove('drag-target')); state.draggedId = null; } async function persistOrder(ordered) { const desiredIds = ordered.map(task => task.id); try { await mutateRemote(tasks => { const taskById = new Map(tasks.map(task => [task.id, task])); const reordered = desiredIds.map(id => taskById.get(id)).filter(Boolean); const unseen = tasks.filter(task => !desiredIds.includes(task.id)); tasks.splice(0, tasks.length, ...reordered, ...unseen); }, 'Ändra prioritering'); } catch (error) { setSyncStatus(`Kunde inte spara ordningen: ${friendlyError(error)}`); await loadTasks({ announce: false }); } } function parseTags(value) { return [...new Set(value.split(',').map(tag => tag.trim()).filter(Boolean))].slice(0, 12); } function statusLabel(status) { return { todo: 'Att göra', doing: 'Pågående', done: 'Klar' }[status] ?? status; } function statusOrder(status) { return { doing: 1, todo: 2, done: 3 }[status] ?? 4; } function friendlyError(error) { if (error.message === 'MISSING_TOKEN') return 'en riktig GitHub-token behövs för att redigera.'; if (error.status === 401) return 'GitHub-token saknas eller är ogiltig.'; if (error.status === 403) return 'GitHub-token saknar skrivrättighet till repot.'; if (error.status === 404) return 'datafilen eller repot kunde inte hittas.'; if (error.status === 409) return 'någon annan hann ändra listan. Försök igen.'; if (error.message.includes('Failed to fetch')) return 'GitHub eller datafilen kunde inte nås.'; return error.message || 'ett okänt fel uppstod.'; } function setBusy(isBusy) { elements.refreshButton.disabled = isBusy; elements.addButton.disabled = isBusy; elements.refreshButton.textContent = isBusy ? 'Arbetar…' : 'Uppdatera'; } function setSyncStatus(message) { elements.syncStatus.textContent = message; } function announceStatus(message) { setSyncStatus(`${message} ${formatTime(new Date())}`); } function formatTime(date) { return new Intl.DateTimeFormat('sv-SE', { hour: '2-digit', minute: '2-digit' }).format(date); } function formatDateTime(value) { if (!value) return ''; return new Intl.DateTimeFormat('sv-SE', { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(value)); } function encodeBase64(value) { const bytes = new TextEncoder().encode(value); let binary = ''; for (let index = 0; index < bytes.length; index += 1) binary += String.fromCharCode(bytes[index]); return btoa(binary); } function decodeBase64(value) { const binary = atob(value.replace(/\s/g, '')); const bytes = Uint8Array.from(binary, character => character.charCodeAt(0)); return new TextDecoder().decode(bytes); } function startPolling() { stopPolling(); state.pollTimer = window.setInterval(() => { if (!document.hidden) loadTasks({ announce: false }); }, POLL_INTERVAL); } function stopPolling() { if (state.pollTimer) window.clearInterval(state.pollTimer); state.pollTimer = null; }