name: Regenerate TURN r165 release entries final

on:
  push:
    branches:
      - fix/turn-cache-refresh-r165
    paths:
      - '.github/workflows/turn-r165-regenerate-final.yml'

permissions:
  contents: write

jobs:
  regenerate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: fix/turn-cache-refresh-r165

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Regenerate release entries
        run: node turn/scripts/release.mjs --write

      - name: Verify release-bound import maps
        run: |
          node turn/scripts/release.mjs --check
          node turn-tests/racing-music-production.mjs
          node turn-tests/covered-rendering-production.mjs

      - name: Commit generated entries
        run: |
          git rm .github/workflows/turn-r165-regenerate-final.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add turn/index.html turn-lab/index.html
          git commit -m 'Regenerate release-bound TURN r165 imports'
          git push origin HEAD:fix/turn-cache-refresh-r165
