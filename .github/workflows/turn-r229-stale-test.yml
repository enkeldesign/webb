name: TURN r229 stale regression updater

on:
  push:
    branches:
      - fix/head-start-flying-lap-r229

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  update:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: fix/head-start-flying-lap-r229
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Fix stale release assertion
        run: python3 .github/turn-r229-stale-test.py
      - name: Validate release-bound multi-track regression
        run: |
          node turn-lab/tests/multi-track-production.mjs
          node turn/scripts/release.mjs --check
          git diff --check
      - name: Commit regression fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add turn-lab/tests/multi-track-production.mjs
          git commit -m "Bind multi-track regression to current release"
          git push origin HEAD:fix/head-start-flying-lap-r229
